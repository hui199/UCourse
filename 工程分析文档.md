# UCourse 工程分析文档

## 1. 工程目录结构

```
.
├── CourseDetailFragment.java  # 课程详情展示页面
├── HomeFragment.java          # 根HomeFragment包装类
├── MainActivity.java          # 主活动类
├── ResultFragment.java        # 结果展示页面（主要实现）
├── TimeFragment.java          # 时间选择页面
├── TimeViewModel.java         # 时间数据视图模型
├── home                       # 课程导入和管理相关
│   ├── ColumnMapDialogFragment.java  # 列映射对话框
│   ├── Course.java                    # 课程数据模型
│   ├── CourseAdapter.java             # 课程列表适配器
│   ├── CsvParser.java                 # CSV文件解析器
│   ├── HomeFragment.java              # 课程导入和展示主页面
│   ├── HomeViewModel.java             # 课程数据视图模型
│   └── XlsxLightParser.java           # Excel文件轻量级解析器
├── model                      # 数据模型
│   └── WeekTimeData.java      # 周时间数据模型
├── result                     # 结果生成和展示相关
│   ├── CourseTimeParser.java      # 课程时间字符串解析器
│   ├── RecommendationSolver.java  # 推荐算法求解器
│   ├── ResultFragment.java        # 结果页面（旧版本/空实现）
│   └── SegmentAdapter.java        # 时间段适配器
├── utils                      # 工具类
│   └── PerformanceLogger.java  # 性能日志工具
└── view                       # 自定义视图
    ├── OnTimeSelectListener.java  # 时间选择监听器
    ├── TimeSlot.java             # 时间槽数据类
    └── TimeTableView.java        # 时间表视图
```

## 2. 关键文件功能介绍

### 2.1 核心页面组件

#### `TimeFragment.java`
- **功能**：时间选择页面，允许用户设置自己的空闲时间
- **数据记录机制**：
  - 使用SharedPreferences进行数据持久化，存储在名为"TimeTablePrefs"的shared preferences中
  - 利用Gson库将WeekTimeData对象序列化为JSON字符串进行存储
  - 通过TimeViewModel管理时间数据，实现数据与视图的分离
  - 实现了TimeTableView的回调接口，处理时间段的选择、修改和删除
  - 每次数据变更后会自动保存到SharedPreferences

#### `home/HomeFragment.java`
- **功能**：课程导入和展示的主页面
- **主要功能**：
  - 管理文件选择器，支持Excel和CSV文件导入
  - 使用后台线程池处理文件解析，避免阻塞UI线程
  - 通过HomeViewModel管理课程数据
  - 实现课程列表展示和滑动评分功能
  - 处理列映射对话框的显示和回调

#### `ResultFragment.java`
- **功能**：展示课程推荐方案的结果页面
- **核心功能**：
  - 从HomeViewModel获取课程数据
  - 通过FragmentManager直接获取TimeFragment实例并调用其getCurrentWeekData()方法获取空闲时间数据
  - 运行RecommendationSolver生成推荐方案
  - 展示多个推荐方案并支持查看课程详情

### 2.2 视图模型类

#### `TimeViewModel.java`
- **功能**：管理时间数据的视图模型
- **核心方法**：
  - `getWeekTimeData()`: 获取当前的周时间数据
  - `updateWeekTimeData(WeekTimeData)`: 更新并保存时间数据
  - `clearData()`: 清空时间数据
- **数据存储**：使用SharedPreferences和Gson实现WeekTimeData的持久化
- **数据观察**：通过LiveData机制实现数据的观察和更新

#### `home/HomeViewModel.java`
- **功能**：管理课程数据的视图模型
- **核心方法**：
  - `save()`: 保存课程数据到SharedPreferences
  - `load()`: 从SharedPreferences加载课程数据
  - `updateInterest(String, int)`: 更新特定课程的兴趣分数
- **数据存储**：使用SharedPreferences和Gson实现Course对象列表的持久化
- **数据观察**：通过LiveData机制实现课程列表的观察和更新

### 2.3 数据模型类

#### `model/WeekTimeData.java`
- **功能**：表示一周时间数据的模型类
- **核心功能**：
  - 按日期（"yyyy-MM-dd"格式）存储时间段数据
  - 提供添加、获取、合并时间段的方法
  - 支持按周起始日期和星期索引管理数据
- **内部类**：`TimeRange`表示时间段，包含起始/结束节次和重叠/相邻判断等功能
- **序列化**：实现了序列化接口，支持Gson序列化

#### `home/Course.java`
- **功能**：课程数据模型
- **字段**：id, title, teachers, unit, rawTime, interest等
- **核心方法**：`fromRows()` - 从表格行数据批量创建Course对象
- **特点**：自动生成唯一ID，支持存储从CSV/Excel导入的完整课程信息

### 2.4 核心工具类

#### `result/CourseTimeParser.java`
- **功能**：解析课程时间字符串为TimeSlot对象列表
- **支持格式**：多种时间表示格式，如"周一7-9节"、"周二3-4节/周四7节"等
- **核心方法**：`parse(String)` - 将原始时间字符串解析为标准化的TimeSlot列表
- **处理逻辑**：
  1. 移除噪声信息（如周数范围、教室地点）
  2. 标准化分隔符
  3. 匹配范围格式（如7-9节）
  4. 匹配列表格式（如7,8节）
  5. 去重处理确保时间段不重复

#### `result/RecommendationSolver.java`
- **功能**：课程推荐算法求解器，使用分支限界法生成最优课程组合
- **核心方法**：`solve()` - 执行推荐算法，返回推荐方案列表
- **优化策略**：
  - 预先过滤可行性课程（排除兴趣为0的课程）
  - 优先包含不冲突的高分课程（兴趣为10的课程）
  - 使用DFS深度优先搜索算法
  - 剪枝优化，避免无效搜索路径
  - 多方案管理，保留多个优质方案

### 2.5 自定义视图

#### `view/TimeTableView.java`
- **功能**：可视化展示和交互的时间表视图
- **核心功能**：
  - 绘制时间表网格
  - 显示已选择的时间段
  - 处理用户的时间选择交互
  - 通过OnTimeSelectListener回调通知时间选择事件

#### `view/TimeSlot.java`
- **功能**：表示单个时间槽的数据类
- **属性**：day（星期几）、startSection（起始节次）、endSection（结束节次）
- **索引**：使用0-6表示周一到周日，节次也从0开始索引

## 3. 重点功能分析

### 3.1 Time Fragment的数据记录机制

Time Fragment采用了多层架构的数据记录机制：

1. **数据模型层**：
   - `WeekTimeData`类负责数据结构定义，包含按日期存储的时间段集合
   - `TimeRange`内部类表示具体的时间段（起始节次、结束节次）

2. **数据管理层**：
   - `TimeViewModel`作为中间层，负责数据的存取和更新
   - 使用SharedPreferences进行持久化存储
   - 通过Gson库将对象序列化为JSON字符串

3. **视图交互层**：
   - `TimeFragment`负责界面交互和用户操作
   - 实现`OnTimeSelectListener`接口处理时间选择事件
   - 每次用户操作后通过ViewModel更新数据

4. **存储流程**：
   - 用户选择时间 → TimeFragment捕获操作 → 调用ViewModel更新 → ViewModel序列化数据 → 保存到SharedPreferences

5. **加载流程**：
   - 应用启动 → TimeViewModel初始化 → 从SharedPreferences读取JSON → 反序列化为WeekTimeData对象 → 通过LiveData通知UI更新

### 3.2 Course Fragment的数据导入和处理流程

Course Fragment实现了完整的课程数据导入和处理流程：

1. **文件选择**：
   - 通过Intent启动文件选择器
   - 支持Excel(.xlsx)和CSV(.csv)两种格式

2. **文件解析**：
   - Excel文件：使用`XlsxLightParser`解析xl/sharedStrings.xml和xl/worksheets/sheet*.xml
   - CSV文件：使用`CsvParser`解析（具体实现未详细查看）
   - 解析结果均转换为字符串二维数组形式

3. **列映射处理**：
   - 弹出`ColumnMapDialogFragment`让用户选择各列对应的数据类型（课程名、教师、时间等）
   - 支持记忆用户的映射选择

4. **课程对象创建**：
   - 调用`Course.fromRows()`静态方法
   - 遍历表格数据，创建Course对象并生成唯一ID
   - 设置默认兴趣分数（5分）

5. **数据存储**：
   - 通过`HomeViewModel`将Course列表保存到SharedPreferences
   - 使用Gson进行序列化和反序列化

6. **数据展示**：
   - 通过RecyclerView和CourseAdapter展示课程列表
   - 支持滑动评分功能，实时更新课程兴趣分数

### 3.3 Result Fragment如何获取其他两个Fragment的数据

Result Fragment采用直接访问和ViewModel两种方式获取数据：

1. **获取课程数据**：
   - 使用`ViewModelProvider`获取`HomeViewModel`实例
   - 通过`homeViewModel.courses.getValue()`获取课程列表
   - 这种方式利用了ViewModel的生命周期管理和数据共享特性

2. **获取空闲时间数据**：
   - 通过FragmentManager直接查找TimeFragment：`findFragmentByTag("time")`
   - 调用TimeFragment的`getCurrentWeekData()`方法获取WeekTimeData对象
   - 包含异常捕获，确保即使TimeFragment不可用也能正常运行

3. **数据同步机制**：
   - 数据获取在用户点击"生成推荐方案"按钮时触发
   - 不会实时监听其他Fragment的数据变化
   - 用户需要重新点击按钮获取最新数据生成方案

### 3.4 课程信息处理流程和最终输出

课程信息从导入到最终输出推荐方案的完整流程如下：

1. **数据准备阶段**：
   - 从HomeViewModel获取课程列表
   - 筛选有效课程（排除空标题或空时间的课程）
   - 从TimeFragment获取空闲时间数据

2. **课程预处理阶段**：
   - **课程分组**：将相同时间和分数的课程分为一组
   - **创建组代表**：每组生成一个代表课程，保留原始课程映射
   - **时间解析**：使用CourseTimeParser将课程时间字符串解析为TimeSlot列表
   - **映射构建**：建立课程ID到时间段列表的映射关系

3. **算法求解阶段**：
   - 初始化RecommendationSolver，设置参数（课程、映射、空闲时间、方案数K、时间限制）
   - 执行solve()方法，内部流程：
     1. 过滤可行课程（兴趣>0且在空闲时间内）
     2. 按兴趣分数降序排序
     3. 预先包含不冲突的高分课程（兴趣=10）
     4. 使用DFS深度优先搜索算法寻找最优解
     5. 应用剪枝优化提高效率

4. **结果处理阶段**：
   - 将组课程还原为原始课程列表
   - 按分数排序推荐方案
   - 为每个方案构建详细说明

5. **UI展示阶段**：
   - 在主线程更新UI
   - 为每个推荐方案创建SolutionCard
   - 使用RecyclerView展示时间段和课程信息
   - 支持点击查看课程详情

### 3.5 多方案输出的触发条件

RecommendationSolver采用了严格的多方案管理策略，触发多方案输出的条件包括：

1. **参数设置**：
   - 构造函数中的参数K决定了最大方案数（默认为3）
   - 当K>1时，会尝试生成多个方案

2. **方案质量过滤**：
   - 课程数量过少的方案会被过滤（至少3门课程）
   - 分数过低的方案不会被保留

3. **方案多样性保证**：
   - **子集关系过滤**：如果A方案是B方案的子集且分数更低，则A方案会被排除
   - **时间段冲突处理**：对于有时间冲突的方案，保留分数更高的
   - **重复方案排除**：通过比较占用时间段和分数，排除重复方案

4. **算法搜索策略**：
   - 深度优先搜索会探索不同的课程组合路径
   - 剪枝策略允许算法探索足够多样化的解空间
   - 时间限制（默认20秒）控制搜索广度，防止过度搜索

5. **排序和保留机制**：
   - 按总分降序排序，分数相同则按课时数升序排序
   - 严格保留前K个最优方案，多余方案被移除

## 4. 技术架构总结

### 4.1 架构模式

1. **MVVM架构**：
   - Model: Course, WeekTimeData, TimeSlot等数据模型
   - View: 各个Fragment和自定义视图
   - ViewModel: HomeViewModel, TimeViewModel等

2. **组件化设计**：
   - 功能模块清晰分离（time、home、result等）
   - 工具类独立封装（解析器、求解器等）

3. **数据持久化**：
   - 统一使用SharedPreferences + Gson进行数据存储
   - 避免了复杂的数据库操作

### 4.2 核心算法

1. **分支限界法**：在RecommendationSolver中用于寻找最优课程组合
2. **正则表达式匹配**：在CourseTimeParser中用于解析复杂的时间字符串
3. **深度优先搜索**：用于探索课程组合空间

### 4.3 性能优化策略

1. **后台线程处理**：文件解析和算法求解在独立线程中执行
2. **剪枝优化**：避免无效搜索路径
3. **预处理过滤**：提前排除无效或低质量的课程
4. **缓存机制**：保存课程分组和映射关系，避免重复计算
5. **进度更新**：使用单独线程更新UI进度条，提升用户体验

## 5. 总结与亮点回顾

UCourse是一个功能完整的课程推荐系统，具有以下亮点：

1. **灵活的时间表示支持**：能够解析多种格式的课程时间字符串
2. **高效的推荐算法**：通过分支限界和剪枝优化，在合理时间内生成高质量推荐
3. **用户友好的交互**：支持直观的时间表选择和课程评分
4. **完善的数据管理**：采用MVVM架构和持久化存储，确保数据安全和一致性
5. **多方案输出**：不仅提供最优解，还提供多个备选方案供用户选择

该应用通过三个主要页面（时间选择、课程导入、结果展示）实现了完整的课程推荐流程，满足了用户根据个人时间偏好和课程兴趣进行智能排课的需求。